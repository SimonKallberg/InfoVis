set FLASK_DEBUG=1 
Detta gör att vi kan uppdatera genom webbläsaren utan att starta om flask appen

set FLASK_APP=app.py(namnet på flaskappen).
bestämmer vilken fil vi ska köra flask på.  

ourenv\Scripts\activate
starta envoironment

deactive (om man är inne i env)

pip install (namn på lib)
installerar bibliotek
kräver import i själva python koden för att använda, import x as y betyder att vi kan använda y.function som syntax.

pip list (lista över installerade libs i den nuvarande env).